<%= form_with(model: procuct, local: true) do |form| %>
  <% if procuct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(procuct.errors.count, "error") %> prohibited this procuct from being saved:</h2>

      <ul>
      <% procuct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :vendor %>
    <%= form.text_field :vendor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
